#!/bin/bash
set -euo pipefail

DBPATH=/var/lib/mongodb
REPLSET=rs01

mkdir -p "$DBPATH"
chown -R rocketchat:rocketchat "$DBPATH" || true

exec mongod \
  --dbpath "$DBPATH" \
  --bind_ip 127.0.0.1 \
  --port 27017 \
  --replSet "$REPLSET"
